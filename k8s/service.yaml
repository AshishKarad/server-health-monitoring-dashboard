apiVersion: v1
kind: Service

metadata:
  name: server-dashboard-service

spec:
  type: NodePort

  selector:
    app: server-dashboard

  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30080
